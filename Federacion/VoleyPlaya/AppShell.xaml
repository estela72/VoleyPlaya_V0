<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="VoleyPlaya.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:views="clr-namespace:VoleyPlaya.Views"
    Shell.FlyoutBehavior="Disabled">

    <TabBar>
        <ShellContent
            Title="Federación"
            ContentTemplate="{DataTemplate views:MainPage}"
            Icon="{OnPlatform 'logo_fvpba.png'}" 
            />
        <ShellContent
            Title="Competiciones"
            ContentTemplate="{DataTemplate views:CompeticionesPage}"
            Icon="{OnPlatform 'logo_fvpba.png'}" 
            />
    </TabBar>
</Shell>
