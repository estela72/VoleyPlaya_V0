<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModels="clr-namespace:VoleyPlaya.ViewModels"
             x:Class="VoleyPlaya.Views.ConfiguracionPage"
             Title="ConfiguracionPage">
    <ContentPage.BindingContext>
        <viewModels:ConfiguracionViewModel />
    </ContentPage.BindingContext>
    <VerticalStackLayout>
        <Label 
            Text="Configuración" TextColor="Blue" FontSize="42" Padding="10"
            VerticalOptions="Center" 
            HorizontalOptions="Center" />
        <Frame>
            <Button Text="Tablas de calendarios" 
                    Command="{Binding TablasCalendariosCommand}"/>
        </Frame>
    </VerticalStackLayout>
</ContentPage>